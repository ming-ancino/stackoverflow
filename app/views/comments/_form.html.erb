<h6><%= form_for [@question, @answer] do |f| %>
  <p>
    <%= f.label :comment %><br />
    <%= f.text_area :body , :value => "" %>
  </p>
  <p>
    <%= f.submit "Add comment" %>
  </p>
<% end %>
</h6>

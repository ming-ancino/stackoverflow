
$('.delete_answer' + <%= @answer.id%> ).parent().fadeOut();
var count =  parseInt($('span#answer_count').text());
count --;
$('span#answer_count').text(count)
if(count > 1)
  $('span#answer_string').text("Answers")
else
  $('span#answer_string').text("Answer")

